
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.fox/


# Beads / Dolt files (added by bd init)
.dolt/
*.db
*.db-shm
*.db-wal
.beads-credential-key
node_modules
client/build/
server/dist/
server/coverage/
server/.reports/
*.log

# Fox Suite URL config (deployment-specific, never commit)
fox-suite.config.json

# Local env files (deployment-specific, never commit)
.env
client/.env
server/.env

# Google OAuth credential downloads (never commit)
client_secret_*.json

